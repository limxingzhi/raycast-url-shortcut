{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "shortcut-manager",
  "title": "Shortcut manager",
  "description": "Manage shortcuts over n8n",
  "icon": "extension-icon.png",
  "author": "lxz",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Productivity"
  ],
  "license": "MIT",
  "preferences": [
    {
      "name": "use_302_redirection",
      "title": "Use 302 Redirection",
      "type": "checkbox",
      "default": false,
      "description": "Select the redirection URL with a 302 response or the locally stored destination.",
      "required": true
    },
    {
      "name": "redirection_endpoint",
      "title": "Redirection endpoint",
      "type": "textfield",
      "description": "Your endpoint to redirect.",
      "required": true
    },
    {
      "name": "sync_endpoint",
      "title": "Sync endpoint",
      "type": "textfield",
      "description": "Your endpoint to fetch all endpoints.",
      "required": true
    },
    {
      "name": "delete_endpoint",
      "title": "Delete endpoint",
      "type": "textfield",
      "description": "n8n form to delete an endpoint.",
      "required": false
    },
    {
      "name": "edit_endpoint",
      "title": "Edit endpoint",
      "type": "textfield",
      "description": "n8n form to edit an endpoint.",
      "required": false
    }
  ],
  "commands": [
    {
      "name": "goto-url",
      "title": "Goto URL",
      "description": "Fuzzy searches for a shortcut's key and opens it in chrome.",
      "mode": "view"
    },
    {
      "name": "sync-url-keys",
      "title": "Sync URL keys",
      "description": "Fetches the latest URL keys",
      "mode": "no-view",
      "interval": "2h"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.1",
    "@raycast/utils": "^1.17.0",
    "fuse.js": "^7.1.0",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
